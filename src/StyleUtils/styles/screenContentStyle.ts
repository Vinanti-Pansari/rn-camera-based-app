import {StyleSheet} from 'react-native';
import {AppColors} from '../colors/AppColors';

export const screenStyle = StyleSheet.create({
  mainView: {backgroundColor: AppColors.pictonBlue, flex: 1},
  middleContainer: {flex: 1},
});
